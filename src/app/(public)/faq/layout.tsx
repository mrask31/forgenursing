import { pageMetadata } from '@/lib/seo'

export const metadata = pageMetadata("NCLEX-RN Retake Practice FAQ", "Answers about ForgeNursing practice, Candidate Performance Reports, the 7-day free trial, pricing, and what your practice results mean.", "/faq")

export default function Layout({ children }: { children: React.ReactNode }) { return children }
